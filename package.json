{
  "name": "SwiftSound",
  "version": "1.0.3",
  "type": "module",
  "scripts": {
    "compile": "swift build -c release",
    "codesign": "codesign --timestamp --options runtime --sign \"Developer ID Application: Lucas Eugene M. Van Ammel (8ZRM3D7KC6)\" .build/release/fft",
    "zip": "ditto  -c -k --norsrc --noextattr ./.build/release/fft ./.build/fft.zip",
    "notarize": "xcrun notarytool submit ./.build/fft.zip --keychain-profile \"AC_PASSWORD\" --wait",
    "build": "npm run compile && npm run codesign && npm run zip && npm run notarize",
    "sync-readme": "replace-regex --from=\"\\d+\\.\\d+\\.\\d+\" --to=\"$npm_package_version\" README.md",
    "release": "pnpm run build && bumpp --no-push --all -x \"pnpm run sync-readme\" && git push --follow-tags && gh release create $(git describe --tags --abbrev=0) --generate-notes --notes \"$(git log --pretty='- %s  %h' $(git describe --tags --abbrev=0 HEAD~1 2>/dev/null || git rev-list --max-parents=0 HEAD)..HEAD~1)\" --notes \"$(git log --pretty='- %s  %h' $(git describe --tags --abbrev=0 HEAD~1 2>/dev/null || git rev-list --max-parents=0 HEAD)..HEAD~1)\" && open .build/"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "bumpp": "^12.3.0",
    "replace-regex": "^0.0.3"
  },
  "funding": "https://github.com/sponsors/mesqueeb",
  "repository": {
    "type": "git",
    "url": "https://github.com/mesqueeb/SwiftSound.git"
  },
  "homepage": "https://github.com/mesqueeb/SwiftSound#readme",
  "bugs": "https://github.com/mesqueeb/SwiftSound/issues",
  "packageManager": "pnpm@11.27.0"
}
